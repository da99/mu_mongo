save_to('title') { 'Formerly: Surfer Hearts' }


partial('__nav_bar')


div.content! { 

  app_vars[:hearts].each do |heart|
    div.heart_link {
     h4 heart.title
     div.body { heart.body }
    }
  end
  
} # === div.content!










